export type Staff = { id: string; name: string; role: string; restaurantId?: string }
export type Demand = { id: string; client_name: string; reward_name: string; reward_points: number; created_at: string }
